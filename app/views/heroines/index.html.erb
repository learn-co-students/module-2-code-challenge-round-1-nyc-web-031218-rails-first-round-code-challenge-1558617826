<h2>All Heroines</h2>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name,  heroine_path(heroine)%>
    </li>
    <br>
  <% end %>
</ul>

<%#= button_to "Create New Heroine", new_heroine_path, {method: :GET}%>
<!-- worked at one point, but saw that it did not always redirect button method from default POST to GET, so switched to link -->
<%= link_to "Create New Heroine", new_heroine_path%>